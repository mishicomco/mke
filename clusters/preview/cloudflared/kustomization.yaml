apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# El namespace y el Secret tunnel-credentials los crea bootstrap-preview.sh
# (el Secret viene del <uuid>.json del túnel, que NO se versiona).
resources:
  - configmap.yaml
  - deployment.yaml
